<h1>Contacts</h1>

<%= form_for :contact, url: "/home/index" do |f| %>
	<p>
		<%= f.label :email %><br />
		<%= f.text_field :email %>
	</p>

	<p>
		<%= f.label :message %><br />
		<%= f.text_area :message %>
	</p>

	<p>
		<%= f.submit %>
	</p>

<% end %>